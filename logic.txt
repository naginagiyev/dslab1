In the processor agent (./agents/processoragent.py) I am thinking a logic like this:
So first, we iterate on plan step by step and as the plan is already generated like executable steps, 
We use them as prompt to generate code.
We use ./models/codex.py to generate that.
Then add it, run it and get the output (./tools/notebook.py).
(In the notebook if the output is error we do not increase the cell ID (switch to the next cell), because we need to edit and fix it.)
After getting the output we check, if it has error, that output goes to the reason function which calls codex.py reason function.
It generates a text like this for example:
"As this approach failed, we do this and ...."
Like it was thinking on its own and solving the problem.
Then this reasoning text goes to the generation model again to write corrected code. 
It is replaced with the code that caused error. Saved, ran and see the output again.
This continues as a recursive function until it is successful (output starts with success).