Imports — Use pandas for data manipulation and numpy to support numeric conversions. No other library is needed at this stage, and do not import path variables; dataDir is already available. Do not import os, and do not import one-hot/label encoders separately if using pandas get_dummies.

Load Data — Load the raw data using pd.read_csv with the path dataDir / "ibmchurn.csv". Do not perform any manual path string manipulation or import os.

Transformations — Drop the column 'customerID' immediately, as it is a unique identifier and not predictive. For the following columns with boolean-like values encoded as objects: 'Partner', 'Dependents', 'PhoneService', 'PaperlessBilling', and 'Churn', convert them from 'Yes'/'No' to 1/0 integer values. For the column 'SeniorCitizen', ensure it is integer type (it already is, but cast to int if needed). The column 'TotalCharges' is described as a numeric-like string field with a few non-numeric (blank) values. Convert 'TotalCharges' to numeric using pandas to_numeric with errors='coerce', which will convert blanks to NaN. Then, fill any NaNs in 'TotalCharges' with 0.0, as these correspond to new customers with tenure=0. For categorical columns with three or more unique values: 'gender', 'MultipleLines', 'InternetService', 'OnlineSecurity', 'OnlineBackup', 'DeviceProtection', 'TechSupport', 'StreamingTV', 'StreamingMovies', 'Contract', and 'PaymentMethod', apply one-hot encoding using pandas get_dummies with drop_first=True to avoid multicollinearity. Numeric columns ('tenure', 'MonthlyCharges', 'TotalCharges') do not require scaling, as outlier analysis shows zero outliers and their distributions are reasonable. Do not drop or engineer any feature not justified by the EDA. Do not address class imbalance, as preprocessing does not include sampling. Leave target column 'Churn' for modeling.

Save — Save the processed DataFrame directly to dataDir with the filename 'ibmchurn_processed.csv', appending '_processed' before the file extension. Do not create any subdirectory or perform any string literal path construction. Do not export paths or use os.

Path Usage Rules — Always use dataDir / "ibmchurn.csv" to load and dataDir / "ibmchurn_processed.csv" to save. Never import or reference os or use string-literal paths anywhere. Do not import dataDir; it is available in the execution context. Do not hardcode any file paths as strings. All path construction must use the '/' operator with Path objects.

Code Style Rules — All variable and function names must use camelCase with no underscores. Each major section should include a short, informative section comment. Do not use inline comments. Do not define variable names with underscores. All code should be clear and conform to these guidelines.
